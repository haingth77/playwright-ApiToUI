import { test, expect } from '@fixtures/login.fixture';

test.describe(`Verify Leave Entitlement page`, async () => {
    test(`LEV-EN-001: Verify that 'Add Entitlement' functionality works correctly`, async ({ loginPage }) => {
        // https://opensource-demo.orangehrmlive.com/web/index.php/api/v2/pim/employees?nameOrId=a&includeEmployees=currentAndPast

        
    });
});